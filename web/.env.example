# Copy to `.env.local`. Base URL of the agent's FastAPI adapter (server.py).
# Local dev (8000, not 8080 - port 8080 is commonly held by Apache on Windows):
AGENT_URL=http://localhost:8000
# Cloud Run: AGENT_URL=https://<your-agent-service>.run.app
